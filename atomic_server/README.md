# Atomic Server

Atomic Server is a graph database with realtime sync and a web app for managing Atomic Data.

The add-on stores data under Home Assistant's persistent add-on data directory.

Default local URL:

```text
http://homeassistant.local:9883/
```

For production use, put the add-on behind HTTPS with a real domain or trusted local certificate.
